# ETTh1 与 Weather 泛化测试

## 目的

本测试不是为了证明模型“能跑”，而是检查当前复现是否具备投入真实时间序列应用的基本条件。

- ETTh1：使用 7 个数值变量预测变压器油温 `OT`。
- Weather：使用训练段内与目标最相关的 8 个变量预测气温 `T (degC)`。
- 数据按时间顺序切分，前 70% 建立记忆，之后 2000 个点测试。
- 测试期间模型冻结，不允许边测试边学习。
- 特征筛选和分箱边界只由训练段计算。
- 同时比较持久性、日周期和训练均值三个简单基线。

这里的 `sstd-memory` 是根据论文 SSTD 思想实现的紧凑稀疏编码检索版本，不是作者发布的完整连续时间 SNN，也不应被表述为论文原模型的最终性能。

## 结果

| 数据集 | 预测跨度 | SSTD MAE | 持久性 MAE | 日周期 MAE | 判断 |
| --- | ---: | ---: | ---: | ---: | --- |
| ETTh1 | 1 小时 | 3.679 | 0.410 | 1.489 | 明显落后 |
| ETTh1 | 24 小时 | 4.913 | 1.490 | 1.490 | 明显落后 |
| Weather | 10 分钟 | 0.686 °C | 0.219 °C | 2.569 °C | 明显落后 |
| Weather | 24 小时 | 3.526 °C | 2.743 °C | 2.743 °C | 落后 |

Weather 还测试了每 6 点保存一个记忆样本的资源受限版本。它的一步和 24 小时 MAE 分别为 0.643 °C 和 3.167 °C，仍未击败持久性基线。增加到全部 3.6 万条训练记忆没有改善结果。

## 目前能得出的结论

当前复现能处理离散序列记忆和受控合成任务，但不能直接投入 ETTh1 或 Weather 这类真实多变量预测任务。它在短期平滑序列上甚至没有超过“下一刻等于当前值”，因此尚未达到最基本的应用门槛。

这不等于已经否定论文中的完整模型。当前实现为了处理高维数据，将完整脉冲动力学简化为 SSTD 编码加近邻检索；论文没有在 ETTh1 和 Weather 上报告结果，也没有给出这些任务的官方适配方式。严格结论应是：**当前复现和当前适配方案泛化失败，论文方法能否通过重新设计连续值编码、读出层和多步训练取得竞争力，仍需进一步实验。**

## 下一阶段

1. 增加只使用训练集拟合的线性自回归基线，建立比持久性更有意义的最低标准。
2. 将连续值分箱改为局部连续编码，让相邻数值产生相似而非互不相关的编码。
3. 学习变化量而非绝对值，减少 ETTh1 训练段和测试段分布漂移的影响。
4. 使用滚动多步预测和标准 96/192/336/720 步协议，与公开预测模型结果对齐。
5. 在性能达到基线后，再测试异常段、缺失值、概念漂移、推理延迟和内存占用。

## 运行

在 PowerShell 中执行：

```powershell
# Run from the cloned repository root.
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\run_real_world_benchmarks.ps1
```

输出保存在 `results` 文件夹。
